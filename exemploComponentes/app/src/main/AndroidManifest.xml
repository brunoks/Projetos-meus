<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brunovieira.exemplocomponentes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/sd" />

        <activity android:name=".Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".adicionarCarroActivity"
            android:label="@string/add_carro" />

        <activity
            android:name=".CadastroClienteActivity"
            android:label="@string/cadastro_clientes" />

        <activity
            android:name=".ClienteActivity"
            android:label="@string/clientes" />
        <activity
            android:name=".ListaCliente"
            android:label="@string/cliente" />
        <activity
            android:name=".CustosActivity"
            android:label="@string/custos" />
    </application>

</manifest>